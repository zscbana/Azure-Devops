# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: $(AGENT_NAME)
  demands:
    - AgentName -equals $(api-key)

stages:
- stage: Test
  jobs:
  - job: Test01
    displayName: Test01
    pool: $(AGENT_NAME)
    steps:
    - script: echo Testing!